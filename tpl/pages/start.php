<?php 

/*
    $allRows = $DB->Select("SELECT * FROM niseuws WHERE docent_id = ? AND nieuws_id = ?", array(20, 1));

    if($allRows){
        while($row = $allRows->fetch_assoc()){
            print_r($row);
        }
    }
    else {
        echo "No rows found";
    }
    */
    //$result = $DB->Exists("SELECT * FROM nieuws WHERE docent_id = ? AND nieuws_id = ?", array(20, 1));
   // print_R($result);
    //$DB->Delete("DELETE FROM nieuws WHERE nieuws_id = ? AND docent_id = ?", array(23, 20));
   //$DB->Insert("INSERT INTO nieuws (docent_id) VALUES (?)", array(19));
   // $DB->Update("UPDATE nieuws SET docent_id = ? WHERE nieuws_id = ?", array(21, 31));

?>
